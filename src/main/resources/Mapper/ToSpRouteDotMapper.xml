<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.electronicGuideSD.dao.ToSpRouteDotMapper">
 	<select id="selectForInt" resultType="java.lang.Integer">
 		select count(*) from to_sp_route_dot rd,scenic_place sp where rd.scePlaId=sp.id
		<if test="name!=null&amp;name!=''">
			and sp.name like'%${name}%'
		</if>
 	</select>
 	<select id="selectList" resultType="com.electronicGuideSD.entity.ToSpRouteDot">
 		select rd.*,sp.name spName from to_sp_route_dot rd,scenic_place sp where rd.scePlaId=sp.id
		<if test="name!=null&amp;name!=''">
			and sp.name like'%${name}%'
		</if>
 		 order by rd.sort limit #{start},#{rows}
 	</select>
 </mapper>